# Root environment variables (optional)
# Individual apps have their own .env files

# Global settings
NODE_ENV=development

# See apps/api/.env.example for backend configuration
# See apps/client/.env.example for frontend configuration
